# Requirements for flux_server.py (local FLUX Schnell image generation).
# These are heavy ML dependencies — install only on the machine running
# the FLUX server, not on the main backend.
#
# Install with:
#   pip install -r requirements-flux.txt --extra-index-url https://download.pytorch.org/whl/cu126
#
# The --extra-index-url flag ensures torch/torchvision pull the CUDA 12.6
# build (required — the default CPU-only build will NOT work).
#
# You also need to accept the FLUX Schnell license on Hugging Face and log in:
#   1. Visit https://huggingface.co/black-forest-labs/FLUX.1-schnell
#      and click "Agree and access repository"
#   2. Run: huggingface-cli login

torch
torchvision
diffusers>=0.31.0
transformers>=4.44.0
accelerate>=0.34.0
bitsandbytes>=0.44.0
peft>=0.12.0
sentencepiece
protobuf
fastapi
uvicorn[standard]
